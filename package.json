{
  "name": "candle-mcp-server",
  "version": "0.1.0",
  "description": "MCP server for searching relationship prompts for Candle",
  "license": "MIT",
  "author": "Candle",
  "type": "module",
  "bin": {
    "candle-mcp-server": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc && shx chmod +x dist/*.js",
    "prepare": "npm run build",
    "watch": "tsc --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.1",
    "dotenv": "^16.5.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "shx": "^0.4.0",
    "typescript": "^5.6.2"
  }
}
